<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#1A1A1A">

    <!-- 标题栏 -->
    <TextView
        android:id="@+id/tv_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="USB调试器"
        android:textSize="20sp"
        android:textColor="@android:color/white"
        android:gravity="center"
        android:padding="16dp"
        android:background="#2D2D2D" />

    <!-- USB图标 -->
    <ImageView
        android:id="@+id/iv_usb_icon"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:layout_centerHorizontal="true"
        android:layout_below="@id/tv_title"
        android:layout_marginTop="40dp"
        android:src="@android:drawable/ic_menu_compass"
        android:scaleType="centerInside"
        android:contentDescription="USB图标" />

    <!-- 连接状态 -->
    <TextView
        android:id="@+id/tv_connection_status"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_below="@id/iv_usb_icon"
        android:layout_marginTop="24dp"
        android:text="未连接USB设备"
        android:textSize="20sp"
        android:textColor="@android:color/white" />

    <!-- 设备列表标题 -->
    <TextView
        android:id="@+id/tv_device_list_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/tv_connection_status"
        android:layout_marginTop="32dp"
        android:layout_marginStart="16dp"
        android:text="已连接设备"
        android:textSize="16sp"
        android:textColor="#B0B0B0" />

    <!-- 设备列表 -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_devices"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/tv_device_list_title"
        android:layout_marginTop="8dp"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="16dp" />

    <!-- 空状态提示 -->
    <TextView
        android:id="@+id/tv_empty_hint"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/tv_device_list_title"
        android:layout_marginTop="16dp"
        android:layout_centerHorizontal="true"
        android:text="没有发现USB设备\n请连接USB设备后刷新"
        android:textSize="14sp"
        android:textColor="#757575"
        android:gravity="center" />

</RelativeLayout>